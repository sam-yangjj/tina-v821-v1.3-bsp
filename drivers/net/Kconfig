# SPDX-License-Identifier: GPL-2.0

menu "Network Device Drivers"
	depends on NET

if NETDEVICES

source "$(BSP_TOP)drivers/net/wireless/Kconfig"
source "$(BSP_TOP)drivers/net/phy/Kconfig"

endif

endmenu
